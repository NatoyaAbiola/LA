class Square {
  constructor(length, x, y) {
    this.radius = radius
    this.x = x || 0
    this.y = y || 0
  }

  area() {
      return this.length * this.length;
    }

perimeter() {
    return 2 * this.height + 2 * this.width;
  }
}
