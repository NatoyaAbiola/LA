<!DOCTYPE html>
<html>
  <head>
    <title>Positions</title>
    <link href="/styles.css" rel="stylesheet">

  </head>
  <body>
    h1><%= Position %></h1>
    <ul>
      <% @team.each do |team_name, player| %>
      <% @player.each do |position, name| %>
        <li><%= player.name %><%= player.team_name%></li>
      <% end %>
    </ul>
</head>
